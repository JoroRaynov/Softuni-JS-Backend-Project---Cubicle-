<main>
    <h1>{{cube.name}}</h1>
    <img class="cube"
        src="{{cube.imageUrl}}">
    <div class="details">
        <p><span>Description:</span>{{cube.description}}</p>
        <p><span>Difficulty level:</span> {{cube.difficultyLevel}}</p>
        <a class="btn" href="/">Back</a>
        {{#if isOwner}}
        <a class="btn" href="/cube/{{cube._id}}/edit">Edit</a>
        <a class="btn" href="/cube/{{cube._id}}/delete">Delete</a>
    {{/if}}
        <a class="btn" href="/accessory/{{cube._id}}/attach">Attach</a>
    </div>
    <h2>Accessories</h2>
    {{#if cube.accessories}}
    {{#each cube.accessories}} 
    <div class="accessories">
        <div class="accessory">
            <img src="{{imageUrl}}" alt="stickerName">
            <h3>{{name}}</h3>
            <p>{{description}}</p>
        </div>

    {{else}}
        <h3 class="italic">This cube has no accessories yet...</h3>
        {{/each}}
        {{/if}}
</main>